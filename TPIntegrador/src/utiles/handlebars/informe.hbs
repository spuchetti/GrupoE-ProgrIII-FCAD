<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            color: #333;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }

        .reserva {
            margin-bottom: 25px;
            padding: 15px;
            border: 1px solid #ddd;
            page-break-inside: avoid;
            background: #f9f9f9;
        }

        .servicios {
            margin-top: 10px;
        }

        .servicio-item {
            padding: 5px 0;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
        }

        .total {
            font-weight: bold;
            margin-top: 10px;
            padding-top: 10px;
            border-top: 2px solid #333;
        }

        .fecha {
            color: #666;
            font-size: 12px;
        }

        .detalles p {
            margin: 5px 0;
        }

        .servicios-header {
            font-weight: bold;
            border-bottom: 1px solid #ccc;
            padding-bottom: 5px;
        }

        .importe-section {
            background: #f0f0f0;
            padding: 10px;
            margin-top: 10px;
            border-radius: 5px;
        }

        .importe-line {
            display: flex;
            justify-content: space-between;
            padding: 3px 0;
        }

        .importe-total {
            border-top: 2px solid #333;
            margin-top: 8px;
            padding-top: 8px;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="header">
        <h1>Reporte de Reservas - Sistema de Salones</h1>
        <p>Generado el: {{fechaGeneracion}} | Total de reservas: {{totalRegistros}}</p>
    </div>

    {{#each reservas}}
    <div class="reserva">
        <h3>Reserva #{{reserva_id}} - {{fecha_formateada}}</h3>

        <div class="detalles">
            <p><strong>Cliente:</strong> {{usuario_nombre_completo}}</p>
            <p><strong>Salón:</strong> {{salon_titulo}} - {{salon_direccion}}</p>
            <p><strong>Capacidad:</strong> {{salon_capacidad}} personas</p>
            <p><strong>Turno:</strong> {{turno_formateado}}</p>
            <p><strong>Temática:</strong> {{tematica}}</p>
        </div>

        <div class="servicios">
            <div class="servicios-header">Servicios Contratados:</div>
            {{#each serviciosArray}}
            <div class="servicio-item">
                <span>{{descripcion}}</span>
                <span>{{importe_formateado}}</span>
            </div>
            {{else}}
            <div class="servicio-item">No hay servicios contratados</div>
            {{/each}}
        </div>

        <div class="importe-section">
            <div class="importe-line">
                <span>Importe Salón:</span>
                <span>{{importe_salon_formateado}}</span>
            </div>

            {{#if serviciosArray}}
            <div class="importe-line">
                <span>Subtotal Servicios:</span>
                <span>{{importe_servicios_formateado}}</span>
            </div>
            {{/if}}

            <div class="importe-line importe-total">
                <span>Importe Total de la Reserva:</span>
                <span>{{importe_total_formateado}}</span>
            </div>
        </div>
    </div>
    {{/each}}

    <div class="header total">
        <h3>Total General: {{formatearMoneda totalGeneral}}</h3>
    </div>
</body>

</html>
